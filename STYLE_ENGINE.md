# מנגנון הסגנון — חוברת משפט פיתגורס

מטרת המנגנון: לשמור את סגנון העבודה של המשתמש בתוך הריפו, להפוך תיקונים חוזרים לכללים קבועים, ולהחיל כל כלל חדש בדיוק בהיקף הנכון בלי לפגוע בדפים שכבר תוקנו ואושרו.

## עיקרון על — מקומי כברירת מחדל
שינוי ממוקד בעמוד אחד נשאר מקומי לעמוד הזה. שינוי משותף אינו ברירת מחדל, גם אם הוא נראה אלגנטי יותר בקוד. עוברים לשכבה משותפת רק כאשר ברור שהכלל באמת משותף, ידוע אילו דפים מושפעים ממנו, וכל עמוד נעול שנמצא בטווח ההשפעה נבדק מחדש.

## AAA Mobile Reader — תצוגת המובייל המאושרת
זהו reference קבוע שאסור לשבור בשינוי עתידי. `SOURCE_OF_TRUTH.md` הוא הסמכות, ו־`STYLE_PROFILE.json.mobileReference` הוא הייצוג הקריא למכונה.

כאשר המשתמש אומר בפרויקט אחר "תעשה כמו aaa" או "כמו פיתגורס בנייד", הכוונה היא לעקרונות האלה:
- mobile-first קריא; לא A4 שלם מוקטן.
- `width:100%`, `min-width:0`, ללא גלילה אופקית וללא חיתוך צדדים.
- grids ועמודות עוברים reflow או נערמים לפי רוחב המסך.
- תמונות, SVG וטבלאות נשארים בתוך `max-width:100%`.
- מצב `mobile-reader` נקבע מיד ב־`index.html`, לפני טעינת החוברת ו־MathJax.
- כל שינוי CSS/JS ליבה שמטרתו להגיע מיד למשתמש כולל version bump מתאים ב־`index.html` כדי לעקוף cache ישן.
- הדפסה ומסך רחב שומרים A4; מובייל רשאי להתארך כדי להישאר קריא.

אסור להחזיר במובייל `transform:scale()`/`zoom` של A4 שלם כפתרון הראשי, wrapper רחב מה־viewport, `white-space:nowrap` שמכריח חיתוך, או הפעלה מאוחרת של מצב המובייל.

## סדר עבודה מחייב לכל שינוי
1. קוראים את `SOURCE_OF_TRUTH.md`, את `STYLE_PROFILE.json` ואת `WORKBOOK_MANIFEST.json`.
2. מריצים `node tools/page-target.js <מספר עמוד>` כדי לזהות את קובץ ה־HTML, קובץ ה־CSS ומצב הנעילה של עמוד היעד.
3. בודקים את דף היעד ואת שכבות ה־CSS שעליהן הוא תלוי לפני כתיבה.
4. מגדירים מראש את **טווח השינוי**: אילו קבצים מותר לשנות ואילו קבצים מחוץ לתחום.
5. מסווגים את הדרישה:
   - **מקומית** — תוכן או פריסה של עמוד אחד. זו ברירת המחדל.
   - **משפחתית** — כלל שמוכח כשייך לקבוצת דפים בעלת root class משותף וברור.
   - **גלובלית** — כלל שחייב להיות נכון בכל 53 הדפים. משתמשים בו רק כשאין פתרון מקומי/משפחתי נכון יותר.
6. שינוי תוכן מתמטי לעולם אינו מופץ אוטומטית. נוסחאות, נתונים, תשובות, שרטוטים וסדר תרגילים נשמרים אלא אם המשתמש ביקש לשנותם במפורש.
7. שינוי ב־`styles/a4-base.css`, ב־`styles/topics/*`, ב־`styles/pythagoras-workbook.css` או ב־`pythagoras-workbook.js` נחשב **שינוי בעל טווח פגיעה רחב**. לפני פרסום חייבים לאמת מחדש כל עמוד נעול שתלוי בשכבה הזאת ולעדכן את `meta/approved-page-locks.json`.
8. אם השינוי משפיע על מובייל, בודקים גם שהוא משמר את `AAA Mobile Reader`: אין overflow אופקי, אין A4-scale במובייל, ה־bootstrap המיידי נשאר פעיל, וכל נכס ליבה ששונה מקבל version bump ב־`index.html`.
9. CSS של עמוד בודד חייב להיות תחום ל־root class של אותו עמוד, למשל `.page-634`. אסור להוסיף בקובץ עמוד selector גלובלי כגון `.page-title`, `.header-container`, `body` או `:root` ללא תחימת העמוד.
10. מריצים:
   - `node tools/style-audit.js`
   - `node tools/repo-guard.js`
   - `node tools/change-scope-audit.js` כאשר יש `BASE_SHA` זמין.
11. בודקים בפועל את דף היעד. אם השתנתה שכבה משותפת, בודקים גם את כל הדפים הנעולים שה־CI דורש לאמת מחדש.
12. רק אחרי הבדיקה מפרסמים לאותו קישור Production קבוע ומאמתים את התוצאה החיה.

## נעילות עמוקות
`meta/approved-page-locks.json` אינו מקור אמת נוסף. הוא נגזרת טכנית של `SOURCE_OF_TRUTH.md`.

נעילת עמוד כוללת שני סוגי חתימות:
- **קובצי העמוד** — HTML ו־CSS ייעודי.
- **תלויות משותפות** — CSS/JS משותפים שמשפיעים על התצוגה של אותו עמוד.

לכן שינוי בקובץ משותף אינו יכול יותר לעקוף את הנעילה. אם תלות משותפת השתנתה, הבדיקה נכשלת עד שמבצעים אימות מחדש של העמוד ומעדכנים את החתימה.

ניסוחי הנעילה עצמם חייבים להיות תואמים למקור האמת. בפרט, אסור שנעילה תטען שהמובייל המאושר הוא A4 מוקטן; המובייל המאושר הוא responsive reflow קריא.

## מניעת זליגת CSS בין עמודים
החוברת המאוחדת טוענת מספר שכבות CSS באותו מסמך, ולכן selector לא־מתוחם הוא סיכון ארכיטקטוני.

כללי ברזל:
- `styles/pages/עמוד-N.css` — selectors ייעודיים חייבים להיות תחת `.page-N`.
- `styles/topics/*.css` — selectors מבניים חייבים להיות תחת root class משפחתי ברור כגון `.pyt-foundation` או `.geo7-page`.
- `styles/a4-base.css` — המקום היחיד לרכיבי בסיס גלובליים אמיתיים.
- `styles/pythagoras-workbook.css` — עוסק רק ב־shell של החוברת או בחריגים שמתחילים ב־`.pythagoras-workbook-shell`.
- אין להוסיף כלל גלובלי לקובץ עמוד כדי לפתור בעיה מקומית.

## איך המנגנון "לומד"
הפרופיל אינו ניחוש. הוא נבנה מדרישות מפורשות שהמשתמש נותן במהלך העבודה. כאשר דרישה חדשה חוזרת על עיקרון קיים, מחזקים את הכלל הקיים ולא יוצרים כלל כפול. כאשר דרישה חדשה סותרת כלל ישן, מעדכנים את הכלל הישן כך שתישאר גרסה אחת בלבד.

דוגמאות:
- "הנקודה צריכה להיות בדיוק בשורה של המלל בכל הפרויקט" → כלל גלובלי רק לאחר בדיקה שהוא אינו משנה דפים נעולים בצורה לא רצויה.
- "סימן המעלות לא במקום הנכון, שלא יקרה שוב" → רכיב יחידות מתמטיות משותף + אימות מחדש של הדפים הנעולים שתלויים בו.
- "בעמוד הזה הכותרת תהיה..." → שינוי מקומי בלבד.
- "מחק מילים כמו זיהוי/מיון/בנייה" → כלל שפה גלובלי, אך עריכת הטקסט מתבצעת רק בעמודים שנמצאים במפורש בטווח העבודה.
- "ככה אני רוצה תמיד בנייד" → נועלים את תוצאת המובייל המאושרת כ־`AAA Mobile Reader`, מנקים ניסוחים סותרים בנגזרות, ומשמרים אותה בכל שינוי עתידי.

## עקרונות הפצה
- **מקומי קודם** כאשר הבקשה מקומית.
- **משפחתי** רק אם יש root class משותף ברור והכלל באמת זהה לכל המשפחה.
- **גלובלי** רק כאשר זו דרישה גלובלית אמיתית ולא קיצור דרך טכני.
- אין "שיפור נלווה" של עמוד שלא נתבקש.
- אין שינוי של דף נעול כתוצאה מתיקון בעמוד אחר.
- אין קוד תיקון ידני שחוזר בעמודים רבים אם אפשר לבנות primitive משותף — אבל primitive משותף נכנס רק אחרי בדיקת טווח פגיעה.

## קבצי המנגנון
- `SOURCE_OF_TRUTH.md` — הסמכות היחידה לדרישות הפרויקט, כולל `AAA Mobile Reader`.
- `STYLE_PROFILE.json` — פרופיל סגנון ותהליך קריא למכונה; `mobileReference` הוא נגזרת מפורשת של סטנדרט המובייל המאושר.
- `STYLE_ENGINE.md` — תהליך העבודה והפצת השינויים.
- `WORKBOOK_MANIFEST.json` — 53 הדפים הפעילים וסדרם.
- `meta/approved-page-locks.json` — חתימות הגנה נגזרות על דפים שאושרו ועל התלויות המשותפות שלהם.
- `tools/page-target.js` — איתור מהיר של קבצי עמוד לפי מספר בחוברת.
- `tools/style-audit.js` — בדיקות תוכן וסגנון קיימות.
- `tools/repo-guard.js` — בדיקות ארכיטקטורה, runtime ונעילות עמוקות.
- `tools/change-scope-audit.js` — בדיקת טווח שינוי מול commit הבסיס.

כל שינוי עתידי חייב לעבור את המסלול הזה לפני פרסום.
