# מנגנון הסגנון — חוברת משפט פיתגורס

מטרת המנגנון: לשמור את סגנון העבודה של המשתמש בתוך הריפו, להפוך תיקונים חוזרים לכללים קבועים, ולהחיל כל כלל חדש בדיוק בהיקף הנכון בלי לפגוע בתוכן המתמטי.

## סדר עבודה מחייב לכל שינוי
1. קוראים את `SOURCE_OF_TRUTH.md`, את `STYLE_PROFILE.json` ואת `WORKBOOK_MANIFEST.json`.
2. בודקים את הדף המבוקש ואת שכבות ה-CSS המשותפות שלו לפני כתיבה.
3. מסווגים את הדרישה:
   - **גלובלית** — כלל שצריך לחול על כל 53 הדפים או על כל רכיב מאותו סוג.
   - **משפחתית** — כלל ששייך לקבוצת דפים/רכיבים בעלי אותה טכנולוגיה.
   - **מקומית** — תוכן או פריסה הייחודיים לעמוד אחד.
4. אם הדרישה מלמדת העדפת סגנון חדשה שניתנת להכללה, מעדכנים את `STYLE_PROFILE.json` לפני יישום הקוד.
5. מיישמים ברמה המשותפת הגבוהה ביותר האפשרית. אין לשכפל 53 תיקונים אם ניתן לתקן רכיב או stylesheet אחד.
6. לא מפיצים שינוי תוכן מתמטי רק בגלל שהוא נראה דומה. נוסחאות, נתונים, תשובות, שרטוטים וסדר תרגילים נשמרים אלא אם המשתמש ביקש לשנותם.
7. מריצים `node tools/style-audit.js` ובודקים שאין חריגה מכללי הספר.
8. בודקים דף מייצג מכל משפחה שנפגעה, כולל תצוגת A4, RTL והדפסה.
9. רק אחרי הבדיקה מפרסמים לאותו קישור Production קבוע ומאמתים את התוצאה החיה.

## איך המנגנון "לומד"
הפרופיל אינו ניחוש. הוא נבנה מדרישות מפורשות שהמשתמש נותן במהלך העבודה. כאשר דרישה חדשה חוזרת על עיקרון קיים, מחזקים את הכלל הקיים ולא יוצרים כלל כפול. כאשר דרישה חדשה סותרת כלל ישן, מעדכנים את הכלל הישן כך שתישאר גרסה אחת בלבד.

דוגמאות:
- "הנקודה צריכה להיות בדיוק בשורה של המלל בכל הפרויקט" → כלל גלובלי ברכיב המשותף.
- "סימן המעלות לא במקום הנכון, שלא יקרה שוב" → רכיב יחידות מתמטיות משותף + כלל RTL גלובלי.
- "בעמוד הזה הכותרת תהיה..." → שינוי מקומי, אלא אם המשתמש מגדיר במפורש תבנית לכל העמודים.
- "מחק מילים כמו זיהוי/מיון/בנייה" → כלל שפה גלובלי לתלמיד.

## עקרונות הפצה
- **CSS/רכיב משותף קודם** → CSS של משפחה → CSS של עמוד.
- כל תיקון גלובלי חייב להשאיר את 53 הדפים תחת אותו מקור אמת.
- אין ניווט לנושאים אחרים; `aaa` הוא פיתגורס בלבד.
- אין "דמו", תוויות מערכת או רכיבים שנועדו להסביר את מבנה המערכת לתלמיד.
- אין קוד תיקון ידני שחוזר בעמודים רבים אם ניתן לייצר primitive משותף.

## קבצי המנגנון
- `SOURCE_OF_TRUTH.md` — סמכות הפרויקט והכללים המחייבים.
- `STYLE_PROFILE.json` — פרופיל הסגנון הקריא למכונה והעדפות שהתקבעו.
- `STYLE_ENGINE.md` — תהליך הלמידה, הסיווג וההפצה.
- `WORKBOOK_MANIFEST.json` — 53 הדפים הפעילים וסדרם.
- `tools/style-audit.js` — בדיקת עקביות אוטומטית לפני פרסום.

כל שינוי עתידי חייב להתחשב בארבעת הקבצים האלה לפני עריכת דפי החוברת.
