# מנגנון הסגנון — חוברת משפט פיתגורס

מטרת המנגנון: לשמור את סגנון העבודה של המשתמש בתוך הריפו, להפוך תיקונים חוזרים לכללים קבועים, ולהחיל כל כלל חדש בדיוק בהיקף הנכון בלי לפגוע בדפים שכבר תוקנו ואושרו.

## עיקרון על — מקומי כברירת מחדל
שינוי ממוקד בעמוד אחד נשאר מקומי לעמוד הזה. שינוי משותף אינו ברירת מחדל. עוברים לשכבה משותפת רק כאשר ברור שהכלל באמת משותף, ידוע אילו דפים מושפעים ממנו, וכל עמוד נעול שנמצא בטווח ההשפעה נבדק מחדש.

## AAA Exact A4 Preview — תצוגת המובייל המאושרת
`SOURCE_OF_TRUTH.md` הוא הסמכות. `STYLE_PROFILE.json.mobileReference` הוא הייצוג הקריא למכונה.

כאשר המשתמש אומר בפרויקט אחר "תעשה כמו aaa", "כמו פיתגורס בנייד" או "תראה לי בדיוק כמו בהדפסה", הכוונה היא:
- הדף בנייד חייב להיראות בדיוק כמו דף ההדפסה.
- אין responsive reflow בתוך `.a4-page`.
- כותרת שנשארת שורה אחת בהדפסה נשארת שורה אחת גם בטלפון.
- אין שינוי במספר עמודות, בסדר אזורים, בגבהים, ב־padding, ב־font-size, ב־line-height או ב־white-space בגלל רוחב הטלפון.
- `index.html` משתמש ב־layout viewport קנוני `width=821, viewport-fit=cover` כדי לשמר את מבנה ה־A4 ולהציג את כל הקנבס על מסך הטלפון.
- ה־A4 עצמו נשאר 210×297 מ״מ. אם נדרשת התאמת קנה־מידה נוספת, היא חיצונית בלבד לכל הדף כיחידה אחת, עם `zoom` אחיד כאשר נתמך ו־`transform: scale()` כ־fallback.
- המעטפת נשארת ממורכזת, אין גלילה אופקית, והגלילה היא אנכית בלבד בין דפים.
- `pinch-zoom` נשאר אפשרי.
- אסור להחזיר selectors או מנגנון `mobile-reader` שמשנים את מבנה הדף.
- כל שינוי CSS/JS ליבה שמטרתו להגיע מיד למשתמש כולל version bump מתאים ב־`index.html` כדי לעקוף cache ישן.

## ביצוע מהיר, עריכה מהירה וניקוי
- אין להתמהמה בחקירה שכבר נעשתה. קוראים רק את המקורות הנחוצים לשינוי ומבצעים.
- לכל דף פעיל יש מסלול עריכה קצר וברור: HTML פעיל אחד + CSS ייעודי אחד + מפרט במקור האמת. אין ליצור גרסאות מתחרות לאותו דף.
- אחרי תיקון מסירים קוד, CSS, asset או מסמך ישן רק כאשר הוכח שהוא לא בשימוש או שהוא סותר את המימוש המאושר.
- אין לשמור "ליתר ביטחון" שאריות קוד שעלולות לחזור בהמשך ולהפריע לעריכה או לתצוגה.
- מבנה HTML מחולק לאזורים סמנטיים ברורים כדי שניתן יהיה לשנות במהירות שאלה, שרטוט או בלוק בלי לפרק את כל העמוד.
- בדיקות רלוונטיות מריצים פעם אחת אחרי השינוי. לא חוזרים על אותה בדיקה ללא אינדיקציה חדשה.
- מיד בסיום כל תיקון שולחים את הקישור החיצוני הקבוע מהעמוד הראשון; deep-link לדף שתוקן מצורף בנוסף כאשר הוא מועיל.

## סדר עבודה מחייב לכל שינוי
1. קוראים את `SOURCE_OF_TRUTH.md`, את `STYLE_PROFILE.json` ואת `WORKBOOK_MANIFEST.json` במידה הנחוצה לבקשה.
2. משתמשים ב־`tools/page-target.js`/`tools/smart-edit.js` כדי לזהות מיד את קובץ ה־HTML, קובץ ה־CSS ומצב הנעילה של עמוד היעד.
3. בודקים את דף היעד ואת שכבות ה־CSS שעליהן הוא תלוי לפני כתיבה.
4. מגדירים מראש את טווח השינוי: אילו קבצים מותר לשנות ואילו קבצים מחוץ לתחום.
5. מסווגים את הדרישה כמקומית, משפחתית או גלובלית.
6. שינוי תוכן מתמטי לעולם אינו מופץ אוטומטית. נוסחאות, נתונים, תשובות, שרטוטים וסדר תרגילים נשמרים אלא אם המשתמש ביקש לשנותם במפורש.
7. שינוי ב־`styles/a4-base.css`, ב־`styles/topics/*`, ב־`styles/pythagoras-workbook.css`, ב־`styles/workbook-canonical-locks.css`, ב־`index.html` או ב־`pythagoras-workbook.js` נחשב שינוי בעל טווח פגיעה רחב. לפני פרסום מאמתים מחדש את הדפים הנעולים ומעדכנים `meta/approved-page-locks.json`.
8. אם השינוי משפיע על מובייל, בודקים שהוא משמר `AAA Exact A4 Preview`: viewport קנוני 821, אותו layout כמו בהדפסה, אין reflow, אין overflow אופקי ואין חיתוך.
9. CSS של עמוד בודד חייב להיות תחום ל־root class של אותו עמוד.
10. מסירים שאריות שהוכחו כמיותרות או סותרות, ואז מריצים את ה־guards הרלוונטיים פעם אחת.
11. רק אחרי הבדיקה מפרסמים לאותו קישור Production קבוע ומאמתים את התוצאה החיה.
12. **מיד בסיום כל תיקון שולחים למשתמש את `https://aaa-pythagoras.vercel.app/` מהעמוד הראשון.** Deep-link יכול להישלח בנוסף אם הוא מועיל, אך לא במקום הקישור הראשי.

## נעילות עמוקות
`meta/approved-page-locks.json` אינו מקור אמת נוסף. הוא נגזרת טכנית של `SOURCE_OF_TRUTH.md`.

נעילת עמוד כוללת:
- קובצי העמוד — HTML ו־CSS ייעודי.
- תלויות משותפות — CSS/JS/entrypoint שמשפיעים על התצוגה של אותו עמוד.

ניסוחי הנעילה חייבים להיות תואמים למקור האמת. בפרט, אסור שנעילה תטען שהמובייל המאושר הוא responsive reflow. המובייל המאושר הוא **Exact A4 Preview** עם viewport קנוני 821 וללא שינוי מבנה פנימי.

## מניעת זליגת CSS בין עמודים
- `styles/pages/עמוד-N.css` — selectors ייעודיים חייבים להיות תחת `.page-N`.
- `styles/topics/*.css` — selectors מבניים חייבים להיות תחת root class משפחתי ברור.
- `styles/a4-base.css` — רכיבי בסיס גלובליים אמיתיים.
- `styles/pythagoras-workbook.css` — shell של החוברת בלבד או כללים שמתחילים ב־`.pythagoras-workbook-shell`.
- אין להוסיף כלל גלובלי לקובץ עמוד כדי לפתור בעיה מקומית.

## איך המנגנון לומד
הפרופיל נבנה מדרישות מפורשות. כאשר דרישה חדשה מחליפה דרישה ישנה, מתקנים את הכלל הישן במקום להשאיר שתי גרסאות פעילות.

דוגמאות:
- "בעמוד הזה הכותרת תהיה..." → שינוי מקומי.
- "הנקודה צריכה להיות בדיוק בשורה של המלל בכל הפרויקט" → כלל גלובלי לאחר בדיקת טווח השפעה.
- "ככה אני רוצה תמיד בנייד" → נועלים את ההתנהגות ב־`SOURCE_OF_TRUTH.md`, `STYLE_PROFILE.json`, guards ונעילות.
- "אני רוצה לראות בנייד בדיוק כמו שיודפס" → Exact A4 Preview; viewport קנוני 821, אין reflow פנימי, וכל התאמת קנה־מידה היא חיצונית בלבד.
- "בסיום כל תיקון תשלח מיד קישור" → הקישור הקבוע מהעמוד הראשון נשלח מיד אחרי כל תיקון שהושלם.
- "תעבוד מהר ותמחק זבל" → מבצעים במסלול הקנוני הקצר ומסירים רק שאריות שהוכחו כמיותרות או סותרות.

## קבצי המנגנון
- `SOURCE_OF_TRUTH.md` — הסמכות היחידה לדרישות הפרויקט.
- `STYLE_PROFILE.json` — פרופיל סגנון ותהליך קריא למכונה.
- `STYLE_ENGINE.md` — תהליך העבודה והפצת השינויים.
- `WORKBOOK_MANIFEST.json` — 53 הדפים הפעילים וסדרם.
- `meta/approved-page-locks.json` — חתימות הגנה נגזרות על דפים שאושרו ועל התלויות המשותפות שלהם.
- `tools/page-target.js` — איתור מהיר של קבצי עמוד לפי מספר בחוברת.
- `tools/style-audit.js` — בדיקות תוכן וסגנון.
- `tools/repo-guard.js` — בדיקות runtime, מטמון, Exact A4 Preview ונעילות.
- `tools/change-scope-audit.js` — בדיקת טווח שינוי.

כל שינוי עתידי חייב לשמור על Exact A4 Preview אלא אם המשתמש משנה את הדרישה במפורש.
