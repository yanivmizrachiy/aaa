# מקור האמת — חוברת משפט פיתגורס

`yanivmizrachiy/aaa` הוא מקור האמת היחיד של חוברת משפט פיתגורס.

## סמכות, runtime ופרסום
- `SOURCE_OF_TRUTH.md` הוא **הסמכות היחידה** לדרישות, תוכן מאושר, עיצוב והתנהגות.
- `WORKBOOK_MANIFEST.json` הוא מקור ה־runtime היחיד ומכיל בדיוק 53 דפים פעילים ובסדר הקנוני שלהם.
- `index.html` הוא **נקודת הכניסה היחידה** לחוברת המאוחדת.
- `STYLE_PROFILE.json`, נעילות וכלי הבדיקה הם מנגנוני אכיפה נגזרים בלבד; הם אינם מקור אמת נוסף ואסור להם לסתור קובץ זה.
- כתובת Production הקבועה: `https://aaa-pythagoras.vercel.app/`.
- **בסיום כל תיקון** שולחים מיד את הקישור הראשי הקבוע מהעמוד הראשון.
- ה־loader משתמש ב־cache-busting אוטומטי וב־`cache: 'no-store'`.

## חוזה עריכה אטומי — חובה בכל שינוי
- כל שינוי בדף הוא עסקה אחת: קוראים קודם את הכללים הגלובליים ואת סעיף העמוד כאן, משנים את ה־HTML/CSS הדרושים, ומעדכנים באותו change-set את סעיף העמוד ואת הכלל הגלובלי אם נולד כלל חדש.
- **אין שתי אמיתות במקביל.** דרישה חדשה שסותרת דרישה ישנה מחליפה אותה במקום; מוחקים את הניסוח הישן ולא מוסיפים לידו גרסה נוספת.
- מקור האמת מתאר רק את המצב הנוכחי המאושר. היסטוריה, ניסיונות וגרסאות קודמות נשארים ב־Git ואינם מצטברים במסמך הזה.
- לכל עמוד יש סעיף קנוני אחד בלבד. אין שני סעיפים לאותו מספר עמוד ואין הוראה קנונית על עמוד פעיל בקובץ אחר.
- שינוי תוכן או פריסה אינו נחשב גמור אם היישום ומקור האמת אינם תואמים בדיוק. אין מצב שבו הקוד תוקן ואת מקור האמת מעדכנים אחר כך.
- אם שינוי חושף כלל סגנון כללי, רושמים אותו פעם אחת תחת `עקרונות פדגוגיה ועיצוב`; בסעיף העמוד מתארים רק את היישום הייחודי שלו.
- באותו change-set מנקים class, CSS, import, חוזה, lock, בדיקה או הערה ישנים שאינם תואמים עוד למימוש המאושר. אסור להשאיר מנגנון ישן שעלול להחזיר רגרסיה בעתיד.
- מנגנוני האכיפה בודקים את מקור האמת ואינם מחליפים אותו. אם יש סתירה, מתקנים את מנגנון האכיפה לפי הקובץ הזה ולא להפך.
- תיקון מוכן רק כאשר הקוד, מקור האמת ומנגנוני האכיפה מסונכרנים, והמקור החי ב־Production מחזיר את הגרסה החדשה.

## AAA Exact A4 Preview
- בנייד, טאבלט ומחשב מוצג אותו A4 בדיוק כפי שיודפס; אין responsive reflow פנימי.
- נקודת הכניסה משתמשת ב־`width=821`; `.a4-page` נשאר 210×297 מ״מ.
- כותרת, עמודות, טיפוגרפיה וסדר התוכן אינם משתנים בגלל רוחב המכשיר.

## מערכת למידת הסגנון — חוזה מחייב
- טווח העבודה הפעיל כרגע הוא עמודים 1–13.
- כלל סגנון שניתן להכללה נבדק בכל 1–13 כדי שהמשתמש לא יצטרך לחזור עליו בעמוד הבא.
- `tools/first-13-style-audit.js` הוא מנגנון האכיפה הפעיל לטווח זה.
- כל עמוד 1–13 חייב לקבל סעיף מפורש בקובץ זה.

## עקרונות פדגוגיה ועיצוב
- **אין כפל סעיפים:** אחרי תרגול מספיק עוברים לפעולה מתמטית אחרת.
- **אין להשאיר שטח A4 גדול ריק ללא מטרה.** מצמצמים מלל וריווח מיותרים ומשתמשים בשטח לתרגול מועיל.
- **ניצול דף חכם קודם למרכוז.** אין דרישה למרכז דוגמה, שאלה או תרגיל על רוחב הדף. אם רכיב זקוק רק לחלק מן הרוחב, מצמידים אותו לצד המתאים ומשתמשים בשטח הפנוי לתרגול קשור.
- שטח עבודה לבן גדול אינו נחשב ניצול של הדף רק מפני שאפשר לכתוב בו.
- **שטח תשובה מותאם לתשובה הצפויה:** ספרה, שתי ספרות, סימן, מילה או ביטוי קצר מקבלים רוחב מתאים ולא תיבה רחבה קבועה.
- **תוצאת חישוב נמצאת מתחת לתרגיל.** כאשר התלמיד מתבקש לחשב תוצאה של תרגיל קצר, שורת התרגיל מסתיימת ב־`=` ותיבת/שורת התוצאה נמצאת מיד בשורה שמתחת. אין להציב את מקום התוצאה באותה שורה לצד התרגיל. החריג הוא השלמת נוסחה או קשר מתמטי, שבהם ההשלמה היא חלק מן הביטוי עצמו ולא "תוצאת חישוב".
- **הסבר מושגי הוא מודרך ואקטיבי:** כאשר אפשר, התלמיד משלים מילה, מספר, סימן או נוסחה מרכזית במקום לקרוא פסקה פסיבית.
- **פתרון רב־שלבי נכתב אנכית:** כל מעבר בשורה חדשה וסימני `=` מיושרים. הכלל חל גם על דוגמאות וגם על תבניות התשובה של התלמיד.
- אין `תיבה בתוך תיבה` ללא צורך.
- כל A4 נשאר יחידה אחת ללא גלישה או חיתוך.

## עמוד 1 — מושגים בסיסיים
- הכותרת `מושגים בסיסיים`; שורת מושג קומפקטית ומשימת סיום של ציור מרובע עם בדיוק שתי זוויות ישרות.

## עמוד 2 — משולש ישר־זווית
- 6 משולשים לזיהוי, שאלה אמריקאית, שתי משימות בנייה והשלמה על מאונכות.

## עמוד 3 — הניצבים
- העמוד עוסק בניצבים בלבד; ארבע משימות פתיחה שונות, מקרה ניצבים שווים, שתי בניות ושלוש משימות למציאת הניצב השני.

## עמוד 4 — היתר
- זיהוי ישיר מוגבל ל־3 משולשים; אחריו מיון רמזים, יישום במלבן, הסקה מספרית והכללה סימבולית.

## עמוד 5 — ניצבים ויתר: מיישמים
- הסקה הפוכה, תיקון טעות, שלושה כרטיסי לוגיקה ושתי משימות בנייה.

## עמוד 6 — ריבוע של מספר
- 12 חישובי יסוד, השוואות, משימות חשיבה, חישובים מאתגרים, תיקון טעויות ומשימת דפוס.

## עמוד 7 — ריבועים ושורשים
- גשר בין ריבוע לשורש: התאמות, השלמות מעורבות, תיקון מסקנות ושרשרת הפוכה; CSS מקומי ללא reflow.

## עמוד 8 — שורש ריבועי
- תרגול פעולת השורש והבנה שלשורש הריבועי יש ערך לא־שלילי אחד; `√81=9` ולא `±9`.

## עמוד 9 — שורש ומשוואה ריבועית
- הכותרת: `משפט פיתגורס – שורש ומשוואה ריבועית`.
- העמוד מוקדש להבדל בין פעולת שורש לבין המשוואה `x²=a`.
- בראש שתי תיבות השוואה עם השלמות: `√9=3` בלבד, לעומת `x²=9` שמסתעף ל־`x=√9 → x₁=3` ול־`x=-√9 → x₂=-3`.
- שלושת המצבים של `x²=a`: אם `a>0` יש שני פתרונות `x₁=√a`, `x₂=-√a`; אם `a=0` יש פתרון יחיד `x=0`; אם `a<0` אין פתרון במספרים הממשיים.
- תרגול המשוואות כולל מקרים חיוביים, אפס ושליליים; ב־`x²=-7` יורדים שורה וכותבים `אין פתרון`.
- תרגול נפרד של פעולת השורש כולל שורשים חיוביים, `√0`, ושורשים של מספרים שליליים שאינם מוגדרים במספרים הממשיים.
- בסיום מתקנים את הטעויות `√49=±7`, פתרון יחיד בלבד ל־`x²=49`, וניסיון לתת פתרון ל־`x²=-9`.
- כל ההסברים המרכזיים נלמדים דרך תיבות השלמה בגודל חכם.
- המימוש: `עמוד-651.html` + `styles/pages/עמוד-651.css`.

## עמוד 10 — קירוב שורשים
- תחימה בין שלמים, קירוב במחשבון, בדיקת סבירות, תיקון טעויות ומשימת בנייה הפוכה.

## עמוד 11 — משוואה ריבועית וקירוב פתרונות
- הדוגמה `x²=20` קומפקטית וצמודה לימין בחלק העליון ולא ממורכזת על כל רוחב הדף.
- לצד הדוגמה, באותה שורת A4, שני תרגילי המשך מודרכים `x²=14` ו־`x²=45`.
- הדוגמה נכתבת בחמש שורות: `x²=20`, `x=√20`, `x₁≈4.47`, `x=-√20`, `x₂≈-4.47`.
- בנוסף ארבעה תרגילים מלאים עם scaffold של ארבע שורות להשלמה, 4 תחימות, 3 טעויות ומשימת בנייה מלאה.
- אין זוג תשובות אופקי בלבד, אין דוגמה רחבה שמייצרת חלל לבן ואין שטח עבודה לבן גדול.
- המימוש: `עמוד-652.html` + `styles/pages/עמוד-652.css`.

## עמוד 12 — צלע ושטח ריבוע
- פריסה 2×2; כל מקום תשובה מותאם במדויק למה שהתלמיד צריך לכתוב, כולל `36`, `9`, `a²`, `b²`.

## עמוד 13 — מגלים את הקשר
- הכותרת: `משפט פיתגורס – מגלים את הקשר`.
- העמוד תוכנן מחדש כעמוד גילוי מלא המנצל כמעט את כל שטח ההדפסה; אין אזור תחתון גדול ריק ואין כרטיסים מוגדלים רק כדי למלא מקום.
- שלב 1: משולש מדויק `3–4–5` ולצדו שלושה חישובי ריבוע. בכל חישוב השורה היא `3²=`, `4²=` או `5²=` ותיבת התוצאה נמצאת **בשורה מתחת**.
- שלב 2: התלמיד מחשב `3²+4²=` ובנפרד `5²=`, שוב עם התוצאה מתחת, ומשווה בין שתי התוצאות. אחר כך מופיע הקשר `3²+4²=5²` והשלמות קצרות על ניצבים ויתר.
- שלב 3: משולש שני `6–8–10` מאמת שהקשר אינו מקרי. יש ארבעה חישובים שונים: `6²+8²`, `10²`, `10²-8²`, `10²-6²`; בכל אחד התוצאה מתחת לתרגיל. כך נבדקים גם הכיוון הישיר וגם הכיוון ההפוך בלי לחזור על אותו סוג פעולה בלבד.
- שלב 4: מעבר לסימון `a,b,c` במשולש ישר־זווית. התלמיד משלים את `a²+b²=c²`, משלים את ניסוח המשפט, ולבסוף מחיל את המבנה על השלשה `7,24,25`.
- אין להחזיר את המבנה הישן שבו שלושת חישובי `3²,4²,5²` היו עם תיבת תשובה אופקית לצד התרגיל, ואין להחזיר עמוד קצר עם שטח A4 לבן גדול.
- אין breakpoint מקומי ואין reflow; המימוש הפעיל: `עמוד-644.html` + `styles/pages/עמוד-644.css` בלבד.

## עמוד 16 — מהשרטוט למשוואה
- דוגמה אנכית, תרגום שרטוט→משוואה, בחירת `+`/`−`, ארבעה פתרונות מלאים ושתי טעויות לתיקון.

## עמוד 17 — פתרון שורה אחר שורה
- 6 תרגילים עם scaffold אנכי וסימני `=` מיושרים.

## עמוד 18 — מציאת היתר
- 6 תרגילים מלאים בפריסה 2×3 עם פתרון אנכי.

## עמוד 19 — מציאת ניצב
- 6 תרגילים מלאים בפריסה 2×3; ריבוע היתר ראשון בחיסור.

## עמוד 20 — תרגול מעורב
- בחירת פעולה, 6 תרגילים מעורבים, תיקון טעות והסקה.

## דרישות קבועות
עברית RTL מלאה; גרפיקה נקייה ומקצועית; קריאות גבוהה; `AAA Exact A4 Preview`; הדפסה נקייה ב־A4; ניווט קודם/הבא; ללא גלישה, חיתוך, שטח מבוזבז או כפילות פדגוגית.
