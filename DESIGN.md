# עקרונות עיצוב ופדגוגיה

עקרונות מנחים לכל דף בחוברת. אלה כללי איכות — לא נאכפים אוטומטית, אבל הם הסטנדרט שכל דף חדש
או תיקון צריך לעמוד בו.

## Exact A4 Preview

- בנייד, בטאבלט ובמחשב מוצג **אותו דף A4 בדיוק** כפי שיודפס. אין responsive reflow פנימי.
- `.a4-page` נשאר 210 על 297 מ״מ; נקודת הכניסה משתמשת ב-`width=821`.
- כותרות, עמודות, טיפוגרפיה וסדר התוכן אינם משתנים בגלל רוחב המכשיר.
- דף A4 שלם צריך להיראות ללא חיתוך וללא גלילה אופקית. ההתאמה למסך היא **קנה-מידה חיצוני בלבד**
  (זום), שמבוצע ב-`pythagoras-workbook.js` — לא שינוי פריסה.

## ניצול הדף

- **אין להשאיר שטח A4 גדול ריק ללא מטרה.** קודם מצמצמים מלל וריווח מיותרים, ואז משתמשים בשטח
  לתרגול מתמטי מועיל ומגוון. לא מגדילים תיבות או מרווחים כדי "למלא" דף.
- **ניצול דף חכם קודם למרכוז.** אין חובה למרכז דוגמה, שאלה או תרגיל. אם רכיב זקוק רק לחלק מהרוחב,
  מצמידים אותו לצד המתאים ומשתמשים בשטח שהתפנה לתרגול קשור.
- שטח עבודה לבן גדול אינו נחשב ניצול של הדף רק מפני שאפשר לכתוב בו.
- **שטח תשובה מותאם לתשובה הצפויה:** ספרה אחת, שתי ספרות, מספר תלת-ספרתי, מספר שלילי, מילה או
  ביטוי קצר מקבלים רוחב שונה ומתאים; אין רוחב קבוע שרירותי.

## מבנה מתמטי ופתרון

- **סימן הכפל הוא נקודה (·), לא ×.** בכל הפרויקט — בטקסט וב-MathJax משתמשים ב-`\cdot` (למשל `6·6`), לעולם לא ב-`\times` או ב-`×`.
- **תוצאת חישוב נמצאת מתחת לתרגיל.** שורת התרגיל מסתיימת ב-`=` ומקום התוצאה מיד בשורה שמתחת.
  חריג: השלמת נוסחה, סימן, בסיס, מספר מתחת לשורש או קשר מתמטי שבו החסר הוא חלק מהביטוי עצמו.
- **פתרון רב-שלבי נכתב אנכית:** כל מעבר בשורה חדשה, סימני `=` מיושרים, והדוגמה מלמדת בדיוק את
  הפורמט שהתלמיד נדרש לכתוב. אותו כלל חל על דוגמאות פתורות ועל המקום שהתלמיד משלים.
- **הצד השמאלי אינו נעלם באמצע פתרון:** כל עוד נמצאים בשלב של `x²`, כל שורת ביניים מתחילה שוב
  ב-`x²`. רק במעבר להוצאת שורש עוברים ל-`x`.

## שפה והוראה

- **אין כפל סעיפים:** לאחר תרגול מספיק של אותה פעולה עוברים להסקה הפוכה, תיקון טעות, בחירה,
  בנייה, השוואה, יישום או קישור בין ייצוגים.
- **הסבר מושגי הוא מודרך ואקטיבי:** כשהתלמיד יכול להשלים מילה, מספר, סימן או נוסחה מרכזית,
  מעדיפים השלמה משמעותית על פסקה פסיבית.
- **הוראה לתלמיד טבעית וברורה, לא ניסוח דמו.** אומרים במשפט עבודה רגיל מה צריך לחשב או להשלים.
  אין לעטוף שאלה בתוויות מלאכותיות (`נתון:`, `המטרה:`, `שלב 1`) כשהמשפט עצמו ברור בלעדיהן.
- **מונחי זווית מדויקים:** שתי צלעות **יוצרות זווית** (לא "נפגשות בזווית"). במשולש ישר-זווית שני
  הניצבים יוצרים את הזווית הישרה.
- **משולשים אינם מוצגים כולם באותה תנוחה.** כשיש כמה משולשים באותו עמוד, מסובבים/משקפים/משנים
  כיוון באופן משמעותי כדי שהתלמיד ילמד את המבנה המתמטי ולא מיקום חזותי קבוע.
- **סימון מחשבון עקבי:** כשמותר להיעזר במחשבון, משתמשים בניסוח הקבוע `ניתן להיעזר במחשבון.`

## RTL ונגישות

- טקסט עברי משתמש ב-maqaf (`־`) בצירופים כמו "ישר־זווית", "שווה־שוקיים", "אי־שלילי".
- ביטויים מתמטיים ויחידות (מעלות, ס״מ) מבודדים ב-`unicode-bidi: isolate` כדי לשמור על סדר נכון ב-RTL.
- לכל שרטוט SVG יש `role="img"` ו-`aria-label` תיאורי.
